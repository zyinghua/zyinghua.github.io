# Repository instructions

- Keep responses concise and logical.
- After completing requested edits, commit and push to the configured remote unless the user explicitly says not to.
- Do not access any server unless explicitly authorized. The standing push instruction authorizes the Git remote operations needed to synchronize and push these edits.
